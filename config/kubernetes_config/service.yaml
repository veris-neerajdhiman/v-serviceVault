apiVersion: v1
kind: Service
metadata:
  name: vault-service
  labels:
    name: vault-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: vault-service

